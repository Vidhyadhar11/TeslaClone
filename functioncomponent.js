import React from 'react';
import { View, Text} from 'react-native';

const = (props) => {
    return(
        <Text>this is not a component</Text>
    );
};
export default ;


import{ StyleSheet } from 'react-native';

const styles = StyleSheet.create({

});

export default styles;